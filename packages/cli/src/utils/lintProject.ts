// ponytail: thin re-export — lintProject lives in @frames/lint so it's usable without the CLI
export { lintProject, shouldBlockRender } from "@frames/lint";
export type { ProjectLintResult } from "@frames/lint";
